# Runtime-only image: dist is built on the host (host has working egress),
# this stage only serves it — no network RUN steps, base image is cached.
FROM nginx:alpine

WORKDIR /usr/share/nginx/html

RUN rm -rf ./*
COPY dist .
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
